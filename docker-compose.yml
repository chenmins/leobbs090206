version: '3.8'

services:
  leobbs:
    build: .
    container_name: leobbs-forum
    ports:
      - "8080:80"
    volumes:
      - ./persist/cgi-bin:/var/www/html/cgi-bin
      - ./persist/non-cgi:/var/www/html/non-cgi
    restart: unless-stopped
